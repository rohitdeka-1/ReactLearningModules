// Naya.js
function Naya({ oldPrice, newPrice }) {
    return (
      <div>
        <span className="old-price">{oldPrice}</span>
        <span className="new-price">{newPrice}</span>
      </div>
    );
  }
  
  export default Naya;
  